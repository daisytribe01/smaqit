# Task: Create smaq commands file

**ID**: 001
**Status**: new

## Context

Create a smaq commands file that defines commands the agent can use within smaqit-enabled projects. These commands provide shortcuts for common operations. The first command to implement is "smaq.load" which loads the framework and spec templates into the agent's context.

## Acceptance Criteria

- [ ] Create a commands file (location TBD: `.smaqit/commands.md` or similar)
- [ ] Define "smaq.load" command that loads SMAQIT.md and all templates into context
- [ ] Document command format and usage
- [ ] Update copilot-instructions to reference the commands file

## Notes
