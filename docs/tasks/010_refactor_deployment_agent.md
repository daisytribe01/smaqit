# Task: Refactor Deployment Agent with Impl Template

**ID**: 010
**Status**: new

## Context

Use the implementation agent template (from Task 003) to refactor `smaqit.deployment.agent.md`. Ensure the deployment agent conforms to the standardized structure for implementation agents.

## Acceptance Criteria

- [ ] Apply implementation agent template structure to `agents/smaqit.deployment.agent.md`
- [ ] Preserve existing deployment agent functionality and constraints
- [ ] Validate YAML frontmatter follows template format
- [ ] Ensure Role, Input, Output, Constraints sections are properly structured

## Notes

- Depends on Task 003 (Create implementation agent template) being completed first
- Deployment agent consumes infrastructure specs and produces deployment artifacts
